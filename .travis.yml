language: go
sudo: required
env:
  - GO15VENDOREXPERIMENT=1
services:
  - docker
install:
  - curl -L https://raw.githubusercontent.com/experimental-platform/misc/master/go-build.sh | sh
script:
  - curl -L https://raw.githubusercontent.com/experimental-platform/misc/master/buildimg.sh | sh
